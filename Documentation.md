HeliX documentation stub.

Current repo state:
- `source.lua` contains the library runtime
- `example.lua` shows the public API shape
- Built-in themes: `HeliX`, `SolarFlare`, `Verdant`, `Whiteout`, `Redline`
- Private asset overrides: `HELIX_ASSET_ID`, `HELIX_KEY_ASSET_ID`, `HELIX_ASSET_BASE_URL`

Recommended next step:
- host `source.lua` behind your own private loader and replace the placeholder URL in `example.lua`
